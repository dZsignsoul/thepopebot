# Dependency Audit - 2026-05-21

Scope: root `package.json` direct `dependencies` and `devDependencies`.

Method:

- Read root `package.json` and `package-lock.json`.
- Checked latest npm versions with `npm view <pkg> version`.
- Checked security exposure with `npm audit --json`.
- Reviewed upstream release notes, advisories, or package diffs for the five highest-priority dependencies.
- Did not modify `package.json`, `package-lock.json`, or install packages.

## Summary

`npm audit` reports 12 vulnerabilities in the current lockfile: 3 high and 9 moderate. The highest-priority direct dependencies are security-driven rather than only semver-distance-driven.

Notable outdated-but-not-selected packages include `lucide-react` (`0.400.0` locked, `1.16.0` latest), `uuid` (`9.0.1` locked, `14.0.0` latest), `@ai-sdk/react` (`2.0.133` locked, `3.0.190` latest), `ai` (`5.0.131` locked, `6.0.188` latest), `node-cron` (`3.0.3` locked, `4.2.1` latest), `open` (`10.2.0` locked, `11.0.0` latest), and `@xterm/xterm` (`5.5.0` locked, `6.0.0` latest). They should be handled after the security items below.

## next

- Declared current: `15.5.12`
- Locked current: `15.5.12`
- Latest npm version: `16.2.6`
- Category: **MAJOR-REFACTOR**
- Priority reason: direct high-severity audit finding. `npm audit` lists multiple Next.js advisories affecting `15.5.12`, including middleware/proxy bypasses, Server Components denial of service, WebSocket upgrade SSRF, cache poisoning, image optimization denial of service, and XSS cases. npm reports a non-major security fix path at `15.5.18`; npm latest is `16.2.6`.
- Release notes checked:
  - Latest release: https://github.com/vercel/next.js/releases/tag/v16.2.6
  - Version 16 upgrade guide: https://nextjs.org/docs/app/guides/upgrading/version-16
  - Next 16 announcement: https://nextjs.org/blog/next-16
- Upgrade notes:
  - If the goal is only to clear the immediate advisories, first evaluate `15.5.18` as a smaller security backport.
  - Upgrading to latest `16.2.6` is a major framework upgrade. The v16 guide calls out Node.js `20.9.0+`; this package currently declares `node >=18.0.0`, so the engines field and runtime/deployment baseline would need review.
  - Review `next/image` behavior changes: local image query-string allowlisting through `images.localPatterns.search`, changed `minimumCacheTTL`, changed default `imageSizes`, and restricted default `qualities`.
  - Run the official codemod or manually check config changes around Turbopack configuration, `middleware` to `proxy` convention, removed/stabilized experimental APIs, and linting workflow changes.
  - Regression areas: auth middleware/proxy behavior, App Router routes, image handling, WebSocket upgrade handling, CSP nonce usage, and any Server Components/cache behavior.

## drizzle-orm

- Declared current: `^0.44.0`
- Locked current: `0.44.7`
- Latest npm version: `0.45.2`
- Category: **SAFE**
- Priority reason: direct high-severity audit finding, GHSA-gpj5-g38j-94v9, SQL injection via improperly escaped SQL identifiers in versions `<0.45.2`.
- Release notes checked:
  - `0.45.2`: https://github.com/drizzle-team/drizzle-orm/releases/tag/0.45.2
  - Advisory: https://github.com/advisories/GHSA-gpj5-g38j-94v9
- Upgrade notes:
  - The `0.45.2` release is specifically a security fix for escaping in `sql.identifier()` and `sql.as()`.
  - No broad migration notes were listed in the `0.45.2` release. Treat this as a focused security upgrade, but test query generation around custom SQL identifiers, aliases, raw SQL helpers, and migrations.
  - Because Drizzle is still `0.x`, semver minor changes should get normal integration testing even when the release note is security-focused.

## ws

- Declared current: `^8.19.0`
- Locked current: `8.19.0`
- Latest npm version: `8.20.1`
- Category: **SAFE**
- Priority reason: direct moderate audit finding, GHSA-58qx-3vcg-4xpx, uninitialized memory disclosure in `websocket.close()` for unsupported `TypedArray` close reasons.
- Release notes checked:
  - `8.20.1`: https://github.com/websockets/ws/releases/tag/8.20.1
  - Advisory: https://github.com/advisories/GHSA-58qx-3vcg-4xpx
- Upgrade notes:
  - `8.20.1` is a bug/security fix release for close reason handling.
  - The declared range `^8.19.0` already permits `8.20.1`; this should be lockfile-only when dependency files are intentionally updated later.
  - Validate WebSocket server/client close paths, especially any code passing non-string/non-Buffer close reasons.

## monaco-editor

- Declared current: `^0.55.1`
- Locked current: `0.55.1`
- Latest npm version: `0.55.1`
- Category: **MINOR-CODE-CHANGES**
- Priority reason: direct moderate audit finding through bundled `dompurify`. The current latest `monaco-editor@0.55.1` depends on `dompurify@3.2.7`, and npm audit reports multiple DOMPurify advisories affecting `<=3.3.3`.
- Release notes checked:
  - `0.55.1`: https://github.com/microsoft/monaco-editor/releases/tag/v0.55.1
  - DOMPurify advisories shown by audit include GHSA-v2wj-7wpq-c8vv, GHSA-cjmm-f4jc-qw8r, GHSA-cj63-jhhr-wcxv, GHSA-39q2-94rc-95cp, GHSA-h7mw-gpvr-xq4m, GHSA-crv5-9vww-q3g8, GHSA-v9jr-rg53-9pgp, and GHSA-h8r8-wccr-v5f2.
- Upgrade notes:
  - There is no forward npm upgrade available at audit time because `0.55.1` is already latest.
  - npm audit suggests `monaco-editor@0.53.0` because that older version does not depend on DOMPurify, but that is a downgrade and may regress editor behavior or published package fixes.
  - Practical remediation options to evaluate later: wait for a patched Monaco release, test a package-manager override/resolution for DOMPurify if compatible with Monaco's dependency constraints, or downgrade to `0.53.0` only after validating editor language exports and UI behavior.
  - Review whether application code renders Monaco-controlled sanitized HTML in a user-controlled context; that determines the urgency beyond the audit finding.

## drizzle-kit

- Declared current: `^0.31.9`
- Locked current: `0.31.9`
- Latest npm version: `0.31.10`
- Category: **MINOR-CODE-CHANGES**
- Priority reason: direct moderate audit finding through `@esbuild-kit/esm-loader` and nested old `esbuild`. npm audit reports the direct package `drizzle-kit` as affected through its dependency chain.
- Release notes checked:
  - No GitHub release entry was available for `0.31.10`; checked npm package metadata and `npm diff --diff=drizzle-kit@0.31.9 --diff=drizzle-kit@0.31.10 -- package.json`.
  - Related advisory surfaced by audit: https://github.com/advisories/GHSA-67mh-4wv8-2f99
- Upgrade notes:
  - The `0.31.9` to `0.31.10` package diff changes runtime dependencies from `esbuild-register` to `tsx@^4.21.0`, keeps `@esbuild-kit/esm-loader@^2.5.5`, and keeps direct `esbuild@^0.25.4`.
  - Because `@esbuild-kit/esm-loader` remains present in latest `0.31.10`, this upgrade may not fully clear the audit finding without an upstream Drizzle Kit dependency change or a package-manager override.
  - Test `npm run db:generate` and `npm run db:studio` after any future update. Watch for TypeScript loader behavior changes caused by the switch to `tsx`.

